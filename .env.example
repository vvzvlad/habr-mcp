# Habr MCP server configuration.
# Read tools (search/list/get article/comments) work WITHOUT any credentials.
# Only write tools (post comment, vote, bookmark) need HABR_CONNECT_SID + HABR_CSRF_TOKEN.

# Content/flow language (fl) and interface language (hl). Default: ru.
HABR_LANG=ru

# --- Write auth (optional; leave empty for read-only) ---
# How to obtain, while logged in to habr.com in your browser:
#   HABR_CONNECT_SID: DevTools -> Application -> Cookies -> habr.com ->
#                     copy the value of the `connect.sid` cookie.
#   HABR_CSRF_TOKEN:  DevTools -> Network -> trigger any write request (e.g. vote)
#                     -> open it -> Request Headers -> copy the `csrf-token` value
#                     (it equals the `csrf_token` cookie value).
HABR_CONNECT_SID=
HABR_CSRF_TOKEN=
# Name of the CSRF cookie that must echo the token (double-submit cookie).
HABR_CSRF_COOKIE_NAME=csrf_token

# --- Author tools (drafts; optional) ---
# The author tools (create/get/update/delete draft, resolve_hubs, list_flows)
# publish Docmost pages to Habr DRAFTS. They need a full logged-in author session.
# Note: pushing a draft to public ("Опубликовать") is NOT implemented — protocol gap §8.
#   HABR_COOKIE: DevTools -> Network -> open a publication/… request -> Request
#                Headers -> copy the WHOLE `cookie` header string (connect_sid +
#                hsec_id + habrsession_id + …); one connect.sid alone is not enough.
#   (HABR_CSRF_TOKEN above is reused for author requests.)
HABR_COOKIE=
# Value for the `habr-user-uuid` request header (mirrors the habr_uuid cookie).
HABR_USER_UUID=
# Value for the `x-app-version` request header (Habr frontend version).
HABR_X_APP_VERSION=2.329.0
# Base URL to download Docmost image attachments for reupload, e.g. https://docs.example.com
DOCMOST_BASE_URL=
# Bearer token to download Docmost attachments.
DOCMOST_API_TOKEN=

# Optional HTTP or SOCKS proxy URL, e.g. socks5://127.0.0.1:1080
PROXY=

# httpx request timeout in seconds.
REQUEST_TIMEOUT=20

# Browser-like User-Agent (override only if needed).
# USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0 Safari/537.36

# Page size for feeds / search.
PER_PAGE=20
